import React from "react";
import "../App.css";

const Buttons = (props) => {
  return (
    <div className="buttons-container">
      <button className="button-items">Buttons ...</button>
      <button className="button-items">Buttons ...</button>
      <button className="button-items">Buttons ...</button>
    </div>
  );
};

export default Buttons;
